#!/bin/groovy

pipeline {
    agent {
      kubernetes {
        yaml """
apiVersion: v1
kind: Pod
spec:
  containers:
  - name: jnlp
    image: jenkins-cpp-agent:latest
    imagePullPolicy: Never
"""
      }
    }

    stages {
        stage('say hi') {
            steps {
                echo "hi"
            }
        }
    }
}
